# test-puppeteer
Little test to apply a new job.
Projecto to add 5 task in todoist automatically.


1. Firts run "npm install" to add node_modules DIR.
2. After run "node app.js" to run project.

this was created with node js and puppeteer.
